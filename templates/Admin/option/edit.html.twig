{% extends "base.html.twig"%}

{% block title "editer un bien" %}

    {% block body %}
        <div class="jumbotron"> 
            <h1>  Editer une option</h1>
        </div>

        <div class="container mt-4"> 
            {{ include('Admin/option/form.html.twig', {boutton: 'Editer'}) }}
        </div>

    {% endblock %}

